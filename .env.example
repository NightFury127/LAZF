# Database Configuration
# -----------------
# For local development
DATABASE_URL="postgresql://username:password@localhost:5432/lazreus_tech"

# For production with connection pooling (Vercel/Railway)
# DATABASE_URL="postgresql://username:password@pooler.host:port/database?sslmode=require&connection_limit=5&pool_timeout=10"

# Direct connection URL for Prisma migrations (required for connection pooling)
# DIRECT_URL="postgresql://username:password@host:port/database?sslmode=require"

# NextAuth Configuration
# ---------------------
# For local development
NEXTAUTH_URL="http://localhost:3000"
# For production (Vercel/Railway)
# NEXTAUTH_URL="https://your-domain.com"
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="generate-a-secure-random-string"

# Email Service (Resend)
# ---------------------
RESEND_API_KEY="your-resend-api-key"
# Default from email address
EMAIL_FROM="noreply@your-domain.com"

# Application Settings
# ------------------
# For local development
APP_URL="http://localhost:3000"
# For production (Vercel/Railway)
# APP_URL="https://your-domain.com"

# Security Settings
# ---------------
# CORS allowed origins (comma-separated)
CORS_ALLOWED_ORIGINS="https://your-domain.com,https://www.your-domain.com"
# Content Security Policy settings (optional)
# CSP_DIRECTIVES="default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https://res.cloudinary.com;"

# Environment
# ----------
NODE_ENV="development"
# Set to "production" in production environment

# Deployment Platform Specific (uncomment as needed)
# ------------------------------------------------
# Vercel specific settings
# VERCEL_URL=""
# VERCEL_ENV=""

# Railway specific settings
# RAILWAY_STATIC_URL=""
# RAILWAY_PUBLIC_DOMAIN=""
